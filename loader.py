import os 
import zipfile
# import kaggle
from kaggle.api.kaggle_api_extended import KaggleApi

# def download_and_extract_kaggle_dataset(dataset, download_path="data"):

    # api = KaggleApi()
    # api.authenticate()
    # os.makedirs(download_path, exist_ok=True)
    # api.dataset_download_files(dataset, path=download_path, unzip=True)
    # print(f"dataset '{dataset}' downloaded and extracted to '{download_path}'.")

# download_and_extract_kaggle_dataset('shrutisaxena/yoga-pose-image-classification-dataset')